name: 'data_transformation'
version: '1.0.0'

profile: 'data_transformation'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_transformation:

    # -----------------------------------------------------------------------
    # Staging – Corre en Spark, escribe parquet en S3
    # -----------------------------------------------------------------------
    staging:
      +materialized: table
      +file_format: parquet

    # -----------------------------------------------------------------------
    # Intermediate – Corre en Postgres, construye esquema estrella
    # -----------------------------------------------------------------------
    intermediate:
      +materialized: table
      fhv:
        +schema: fhv_intermediate
        +tags:
          - fhv
          - intermediate
      hvfhs:
        +schema: hvfhs_intermediate
        +tags:
          - hvfhs
          - intermediate
      green:
        +schema: green_intermediate
        +tags:
          - green
          - intermediate
      yellow:
        +schema: yellow_intermediate
        +tags:
          - yellow
          - intermediate

    # -----------------------------------------------------------------------
    # Marts – Vistas materializadas para consumo en Power BI
    # -----------------------------------------------------------------------
    marts:
      fhv:
        +materialized: materialized_view
        +schema: fhv_marts
        +tags:
          - fhv
          - marts
      fhvhv:
        +materialized: materialized_view
        +schema: fhvhv_marts
        +tags:
          - fhvhv
          - marts

    # -----------------------------------------------------------------------
    # Warehouse – Corre en Postgres, tablas finales de reporting
    # -----------------------------------------------------------------------
    warehouse:
      fhv:
        +schema: fhv_model
      green:
        +schema: green_model
      hvfhs:
        +schema: hvfhs_model
      yellow:
        +schema: yellow_model
